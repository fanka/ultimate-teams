
<div class="container">
  <h1>Today's Teams:</h1>

  <div>
    <%= button_to 'Generate new pairs', rotate_and_generate_new_path, class: 'rotate-and-generate-new-button' %>
  </div>


    <p><% @pairs.each do |pair| %>

      <%= "Student 1: #{pair.student_one.email} -- Student 2: #{pair.student_two.email}" %></p>
  <% end %>
</div>



<div class="container">
  <% @my_pairs1.each do |pair| %>
    <%= "#{pair.student_one.email} -- #{pair.student_two.email}" %>
  <% end %>
  <% @my_pairs2.each do |pair| %>
    <%= "#{pair.student_one.email} -- #{pair.student_two.email}" %>
  <% end %>
</div>
