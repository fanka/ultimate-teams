<h1>Todays pairs:</h1>
<div><%= button_to 'Generate new pairs' , rotate_and_generate_new_path %></div>
<p><% Pair.all.each do |pair| %>
<%= "Student 1: #{pair.student_one.email} -- Student 2: #{pair.student_two.email}" %></p>
<% end %>
